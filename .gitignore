# Installed dependencies (consumers run `npm ci` / Experience Builder installs them)
node_modules/

# Visual Studio cache / user state
.vs/
*.user
*.suo

# Build output
dist/
build/

# OS / editor cruft
.DS_Store
__MACOSX/
Thumbs.db
*.log

# Community attachment zips built by publish-all.ps1 never get committed
*.zip
